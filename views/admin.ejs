<!DOCTYPE html>
<html lang="ro">
<head>
<meta charset="UTF-8">
<title>Admin - HYGHBYTE</title>
<link rel="stylesheet" href="/styles.css">
<script defer src="/admin.js"></script>
</head>

<body class="admin-page">

<h1>Adaugă joc nou 🎮</h1>

<form action="/admin/add-game" method="POST">

  <label>Tip joc:</label>
  <select name="type" id="typeSelect" required>
    <option value="steam">Steam</option>
    <option value="manual">Itch.io / Altceva</option>
  </select>

  <!-- STEAM ONLY -->
  <div id="steamFields">
    <label>Steam App ID:</label>
    <input type="text" name="steamId" placeholder="Ex: 730">
  </div>

  <!-- MANUAL INPUT -->
  <div id="manualFields" style="display:none;">
    <input name="title" placeholder="Nume joc">
    <input name="genres" placeholder="Genuri (ex: Horror, Indie)">
    <input name="imageUrl" placeholder="Imagine URL">
    <input name="link" placeholder="Link joc">
  </div>

  <label>Featured ⭐</label>
  <input type="checkbox" name="featured">

  <button type="submit" class="admin-btn">💾 Salvează joc</button>
</form>

<hr>

<h2>Jocuri existente</h2>
<div class="admin-list">
  <% db.games.forEach(g => { %>
    <div class="admin-item">
      <img src="<%= g.imageUrl %>" width="70">
      <strong><%= g.title %></strong>
      <form action="/admin/delete" method="POST" style="display:inline;">
        <input type="hidden" name="id" value="<%= g.id %>">
        <button class="del-btn">🗑️ Șterge</button>
      </form>
    </div>
  <% }) %>
</div>

</body>
</html>
