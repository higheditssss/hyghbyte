<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <title>HYGHBYTE Admin</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #0d0d0d;
            color: #fff;
            padding: 20px;
        }
        h1 { margin-bottom: 20px; }
        input, textarea, select {
            width: 100%;
            padding: 10px;
            margin: 8px 0;
            border-radius: 6px;
            border: none;
        }
        button {
            background: #e50914;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            cursor: pointer;
            margin-top: 10px;
        }
        .game-box {
            background: #141414;
            padding: 12px;
            border-radius: 10px;
            margin-top: 10px;
            display: flex;
            justify-content: space-between;
        }
        img {
            width: 90px;
            height: 60px;
            object-fit: cover;
            border-radius: 6px;
        }
        .del-btn {
            background: #b20710;
        }
    </style>
</head>
<body>

<h1>Adaugă joc nou 🎮</h1>

<form action="/admin/add?token=<%= token %>" method="POST">
    <input type="text" name="title" placeholder="Nume joc" required>
    <input type="text" name="genres" placeholder="Genuri (ex: Horror, Indie)" required>
    <input type="url" name="imageUrl" placeholder="Imagine URL" required>
    <input type="url" name="link" placeholder="Link joc" required>
    
    <label>
        <input type="checkbox" name="featured">
        Afișează la Featured ⭐
    </label>

    <button type="submit">💾 Salvează joc</button>
</form>

<hr style="margin:20px 0;border-color:#333;">

<h2>Jocuri existente</h2>

<% games.forEach(g => { %>
<div class="game-box">
    <div>
        <strong><%= g.title %></strong><br>
        <small><%= g.genres %></small>
    </div>
    <img src="<%= g.imageUrl %>">
    <form action="/admin/delete/<%= g.id %>?token=<%= token %>" method="POST">
        <button class="del-btn">Șterge ❌</button>
    </form>
</div>
<% }) %>

</body>
</html>
