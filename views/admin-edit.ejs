<!DOCTYPE html>
<html lang="ro">
<head>
<meta charset="UTF-8">
<title>Editare Joc</title>
<link rel="stylesheet" href="/admin.css">
</head>
<body>

<div class="admin-header">
  Editare: <%= game.title %>
  <a href="/admin-dashboard"><button class="logout-btn">Înapoi</button></a>
</div>

<div class="admin-section">
<form class="add-form" method="POST" action="/admin-edit/<%= game.id %>">

  <input type="text" name="title" value="<%= game.title %>" required>
  <input type="text" name="genres" value="<%= game.genres %>" required>
  <input type="url" name="link" value="<%= game.link %>" required>
  <input type="url" name="imageUrl" value="<%= game.imageUrl %>" required>

  <label><input type="checkbox" name="featured" <%=game.featured?"checked":""%>> ⭐ Featured</label>
  <label><input type="checkbox" name="visible" <%=game.visible?"checked":""%>> 👁 Vizibil</label>

  <button type="submit" class="add-btn">💾 Salvare</button>
</form>
</div>

</body>
</html>
